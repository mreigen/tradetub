<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "BARZiT" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
<!--
      <div id="flash_notice">
        Welcome to the demo application of <a href='http://activeadmin.info'>ActiveAdmin</a>.
        Go <a href='/admin'>try out the Administration interface</a>!
      </div>
-->
      <link href="/stylesheets/active_admin.css?1334730858" media="all" rel="stylesheet" type="text/css">
			<link href="/stylesheets/admin.css?1334207530" media="screen" rel="stylesheet" type="text/css">
			<div id="header"><h1 id="site_title">Barzit</h1><ul class="header-item" id="tabs">
	
			</ul>
			<p class="header-item" id="utility_nav">Help</p></div>
			
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h2, yield(:title) if show_title? %>
      <%= yield %>
    </div>
  </body>
</html>
