<div class="big-title"><%= offerer.username %>'s Bazaar</div>
<h3>Available <a href="javascript:setCurrent('items')">items</a> | <a href="javascript:setCurrent('services')">services</a></h3>
<ul id="their-available-item-list">
<% offerer.products.each do |item| %>
	<% unless offer.offer_items.include?(OfferItem.find_by_product_id(item.id))%>
	<li class="their-item-li">
		<div class="item-image-list"><img src="<%= item.image.url(:thumb) %>" /></div>
		<div class="item-title"><%= item.title %></div>
		<div class="item-description"><%= item.description %></div>
		<div class="item-price"><%= number_to_currency(item.price) %></div>
		<%= hidden_field_tag 'offering' + item.id.to_s + '[price]', item.price %>
	</li>
	<% end %>
<% end %>
</ul>
<ul id="their-available-service-list">
<% offerer.services.each do |service| %>
	<% unless offer.offer_services.include?(OfferService.find_by_service_id(service.id)) %>
		<li class="their-service-li">
			<div><%= service.name %></div>
		</li>
	<% end %>
<% end%>
</ul>

<script language="javascript">
function setCurrent(type) {
	if (type == "items") {
		$("#their-available-item-list").show();
		$("#their-available-service-list").hide();
	}
	else if (type == "services") {
		$("#their-available-item-list").hide();
		$("#their-available-service-list").show();
	}
}
</script>
